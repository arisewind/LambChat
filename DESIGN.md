# LambChat 前端设计系统

本文件是 LambChat 前端视觉与交互实现的项目级基线。所有新页面、组件和 UI 改动都必须先阅读本文件，并优先复用现有组件与设计 token，而不是另起一套视觉语言。

## 产品气质

LambChat 是面向 Agent 工作流的专业生产力产品。界面应当克制、清晰、可信，突出内容与任务状态，而不是装饰。优先级为：可读性 > 信息层级 > 操作效率 > 装饰性。

## 视觉原则

- 使用现有主题 token（尤其是 `--theme-primary` 及相关 CSS 变量），不要在组件中硬编码品牌色。
- 深色和浅色主题都必须可读；文字、边框、图标和状态色满足可感知对比度。
- 通过字号、字重、间距和色彩建立层级，避免依赖大面积渐变、阴影或动画。
- 优先使用单层容器；只有在信息结构确实需要时才使用卡片，避免嵌套卡片和重复边框。
- 保持圆角、间距、控件高度和图标尺寸的一致性，优先复用既有 primitive。
- 避免无意义的 pill、侧边标签、斜体衬线标题、装饰性 emoji、自动播放动效和强烈玻璃拟态。

## 布局与响应式

- 先定义内容宽度、主次栏关系和窄屏行为，再写 CSS。
- 桌面、平板、移动端都要验证；不能只依赖桌面截图。
- 长文本、工具结果、代码、图片和错误状态必须有明确的溢出与折行策略。
- 交互控件必须有足够的点击区域；移动端不依赖 hover 才能完成关键操作。

## 交互与状态

- 每个异步操作至少考虑 loading、success、empty、error 和 disabled 状态。
- 状态变化应靠文字、图标或结构共同表达，不能只靠颜色。
- 动效只用于反馈、层级变化和空间连续性；短、可打断、尊重 `prefers-reduced-motion`。
- 键盘焦点必须清晰可见；弹层、折叠区和菜单需要正确的语义与关闭路径。

## 组件实现

- 先搜索并复用 `frontend/src/components`、`frontend/src/styles` 和现有 token。
- 新增组件前说明它解决的独立问题，避免为单一场景创建重复 primitive。
- UI 文案必须进入 zh / en / ja / ko / ru 五个 locale，不在组件中散落面向用户的硬编码文本。
- 图标使用现有 lucide 约定，保持尺寸、描边和透明度一致。
- 组件测试优先验证用户可观察行为；复杂视觉规则可补充源码结构测试。

## 交付检查

提交前逐项检查：

1. 是否沿用了 `DESIGN.md`、`PRODUCT.md` 和现有组件，而不是重新发明视觉系统？
2. 浅色/深色主题、窄屏、长文本和空/错/加载状态是否可用？
3. 键盘焦点、语义标签、对比度和 reduced motion 是否处理？
4. 是否避免不必要的渐变、嵌套卡片、过度圆角和无目的动效？
5. 是否同步五种 locale，并运行前端测试、lint 和 build？
6. 若已安装 Impeccable，是否运行对应检查器并修复报告的问题？

## Impeccable 使用约定

优先使用 Impeccable 的项目级设计指导和检查器：

```bash
npx impeccable install
/impeccable init
npx impeccable update
```

网络或运行环境不允许时，以本文件和 `PRODUCT.md` 为准，使用上面的交付检查清单完成等价人工检查。Impeccable 是辅助工具，不替代项目现有的 TypeScript、测试、lint、无障碍和构建门禁。
